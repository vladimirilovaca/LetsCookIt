<div id="main-container">
<div id="signup" class="container col-xxl-4">
  <h1 class="text-center mt-5 mb-4">User Register</h1>

  <form class="container-sm mx-auto" style="max-width: 400px;" action="/register" method="POST">
    <div class="input-group mb-2">
      <label class="w-100 mb-2" for="username" style="color: black;">User Name</label>
      <input type="text" name="username"
        class="form-control  py-2 ps-4 {{#if errors.username}}is-invalid{{/if}}" placeholder="Name"
        aria-label="User name" value="{{ user.username }}">
      {{#if errors.username}}<div class="invalid-feedback">{{ errors.username }}</div>{{/if}}
    </div>

    <div class="input-group mb-2">
      <label class="w-100 mb-2" for="email" style="color: black;">Email</label>
      <input type="text" name="email"
        class="form-control  py-2 ps-4 {{#if errors.email}}is-invalid{{/if}}"
        placeholder="Email: muchacho_registrate@example.org" aria-label="User email" value="{{ user.email }}" required>
      {{#if errors.email}}<div class="invalid-feedback">{{ errors.email }}</div>{{/if}}
    </div>

    <div class="input-group mb-3">
      <label class="w-100 mb-2" for="password" style="color: black;">Password</label>
      <input type="password" name="password"
        class="form-control  py-2 ps-4 {{#if errors.password}}is-invalid{{/if}}"
        placeholder="Password 💩" aria-label="User password" required>
      {{#if errors.password}}<div class="invalid-feedback">{{ errors.password }}</div>{{/if}}
    </div>

    <div class="d-grid gap-2">
      <button type="submit" class="btn btn-dark bg-light-green ">Register</button>
    </div>
  </form>
</div>
</div> 

<style>
    #main-container {
      background-image: url('https://res.cloudinary.com/ddtg22atq/image/upload/v1705264088/Letscookit/Post_uadqzh.jpg'); /* Reemplaza 'ruta_de_tu_imagen.jpg' con la ruta real de tu imagen */
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      min-height: 100vh; /* Ajusta la altura mínima para cubrir la totalidad del viewport */
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

  </style>
